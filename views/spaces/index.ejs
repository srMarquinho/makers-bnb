<!DOCTYPE html>
<h1>spaces/index.ejs</h1>

  <form method="get" action="/spaces/booking">
    <button type="submit">Book this space</button>
  </form>
<table>
  <% spaces.forEach(function(space) { %>
  <tr><%=space.name%> - <%=space.description%> - £<%=space.price%></tr>
  <br>
  <% }); %>
</table>
